//====================================================|
// SCORES HANDLER


//--------------------------| Import

import {
  updateScoresByTime,
  updateScoresByGuesses
} from '../actions/scores';


//--------------------------| Export

export default (game) => {
  console.log('SCORES HANDLER', game);

  return {};
};
